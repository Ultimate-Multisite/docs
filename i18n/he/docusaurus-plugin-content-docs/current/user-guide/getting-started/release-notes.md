---
title: הערות שחרור
sidebar_position: 9
_i18n_hash: e9f9d20e55608b81945ab7dfcf495fcb
---
# הערות יציאה

## גרסה 2.12.0 — שוחררה ב-2026-05-15

- חדש: הוספנו תמיכה ב-Hostinger (hPanel) כספק אחסון נתמך, עם אינטגרציית מיפוי דומיין
- חדש: Site Exporter מטפל כעת בחבילות ייבוא רשת (network import bundles) לצורך שחזור אתר ברמת הרשת בצורה יעילה יותר
- תיקון: מיילים רשתיים BCC משתמשים כעת בכותרת `undisclosed-recipients` כדי למנוע חשיפת כתובות הנמענים
- תיקון: תאריך תפוגת המנוי אינו נפגם יותר בעת שמירה עם ערך שאינו תאריך
- תיקון: עדכוני מנוי של Stripe מנקים כעת הנחות באופן נכון מבלי לקרוא ל-API המיושן `deleteDiscount`
- תיקון: הפניות SSO באתרים עם מיפוי דומיין מוגבלות כעת כדי למנוע לולאות הפניה אינסופיות
- תיקון: בחירת תמונה במַדְרֵך ההגדרה מעדכנת כעת נכון את מודל הנתונים הבסיסי
- תיקון: Site Exporter CLI שומר כעת את בחירת אתר ברירת המחדל הנכונה ברמת הרשת
- שיפור: הסרנו את חבילת wp-cli המובנית מהחבילה של ה-plugin, מה שמקטין את גודל ה-plugin

## גרסה 2.11.0 — שוחררה ב-2026-05-11

- חדש: יצוא אתרים כעת אורזים `index.php` שמפעיל את עצמו, כך שניתן להתקין את קובץ ה-ZIP על אחסון חדש ללא התקנת plugin נפרדת.
- חדש: יצוא רשת מאפשר למנהלים לייצא את כל תת-האתרים בארכיון יחיד מדף המנהל של Site Export.
- חדש: ההגבלה על הפעלת תבניות אתר (Allow Site Templates plan toggle) נאכפת כעת באמצעות שרשרת גיבוי, ומגבילה נכון את זמינות התבניות בהתאם למגבלות התוכנית.
- חדש: עורך טופס התשלום מזהיר כאשר מוצר מתווסף ללא הגדרת שדה נדרש.
- חדש: לשונית הגדרות ייבוא/ייצוא מתארת כעת בבירור את היקף פעולותיה ומקשרת ישירות לכלי Site Export.

## גרסה 2.10.0 — שוחררה ב-2026-05-05

- חדש: מַדְרֵך הגדרה מודרך של PayPal להזנת פרטי אימות ידנית, עם שער OAuth להגדרה חלקה של ה-gateway.
- חדש: לוח הלקוחות של מעבר תבניות (Template switch) עוצב מחדש עם כרטיס התבנית הנוכחית, רשת קבועה וכפתור **אפס תבנית נוכחית**.
- תיקון: מעבר תבניות אינו קופא יותר את הממשק המשתמש (UI) במקרה של כשל AJAX.
- תיקון: מצבי הרשאות למעבר תבניות מוגנים מפני גישה בלתי מורשית.
- תיקון: קלטים (inputs) של דריסת אתר (Site override) מאומתים לפני השמירה.
- תיקון: ההודעה לכתובת חיוב מוצגת כעת כאשר הכתובת ריקה.
- תיקון: נפתרו הודעות ההפחתה (deprecation notices) של PHP 8.1 מ-null ל-string.
- תיקון: Currents נטען עצלנית (lazy-loaded) לפני ה-hook `init` כדי למנוע בעיות תזמון.
- תיקון: נתיב SSO המסונן נשמר בכל תהליכי ההתחברות.
- תיקון: אפשרויות זהות לאתר ריק נשמרות בעת השמירה.

## גרסה 2.9.0 — שוחררה ב-2026-04-30

- חדש: הוספנו יצוא וייבוא לאתר יחיד תחת **Tools > Export & Import**.
- תיקון: קובצי ZIP המיוצאים מוגשים כעת דרך נקודת קצה להורדה מאומתת.
- תיקון: תוקנו סיכוני הזרקת SQL ובעיות שאילתה בייבוא/ייצוא ממתינים.
- תיקון: אתר ממתין אינו מפורסם כאשר המנהל מאמת ידנית את אימייל הלקוח.
- תיקון: רשומות `pending_site` יתומות מנוקות כאשר המנוי חסר.
- תיקון: תוקנו המרווחים של ניווט ההגדרות והניווט העוגן של החיפוש.
- תיקון: אתרים ממתינים מוצגים כעת ראשונים בתצוגה "כל האתרים".
- תיקון: הוספה כותרת User-Agent לספק צילומי המסך (mShots) כדי למנוע שגיאות 403.
- תיקון: נפתרה תלות מעגלית בלוח הזמן של קרונו של הייבוא.
- תיקון: מזהי ה-Tour מנורמלים לקו תחתון (underscores) במפתחות הגדרות המשתמש.
- שיפור: הוחלף ZipArchive במקום Alchemy/Zippy לצורך תאימות טובה יותר.

## גרסה 2.8.0 — שוחררה ב-2026-04-29

- חדש: הוספת מתג Jumper לממשק המשתמש של הגדרות אופציות אחרות.
- חדש: הוסף עמודת סטטוס לטבלת רשימת טפסי התשלום.
- חדש: מַעַלֵּג קובץ sunrise עבור הרחבות MU-plugin sunrise מותאמות אישית.
- שיפור: הוסר הגדרת ההרשמה להצגת הודעות שגיאה מדף ההגדרות.
- תיקון: כרטיס האתר בדף תודה — התמונה מוגבלת כעת והקישורים מעוצבים נכון.
- תיקון: ספק צילומי המסך הוחלף מ-thum.io ל-WordPress.com mShots.
- תיקון: Enable Registration ו-Default Role מקבלים כברירת מחדל נכונה בהתקנה חדשה.
- תיקון: `get_site_url()` אינו מחזיר ריק יותר כאשר הדומיין כולל פורט.
- תיקון: קבצי מדיה משוכפלים נשמרים כעת נכון כאשר הגדרת `copy_media` הייתה ריקה.
- תיקון: מטמון אובייקטים (Object cache) מבטל נכון לאחר כתיבת sitemeta ב-network-activate.
- תיקון: דומיין מותאם אישית מקודם אוטומטית לראשי עבור דומיינים מ-3 חלקים.
- תיקון: מנוי ממתין מבוטל כאשר מנקה תשלום שפג.
- תיקון: בודק חוזק הסיסמה נקשר מחדש לאחר סגירת ה-prompt של ההתחברות המובנית.
- תיקון: מניעת טעינת דף אינסופית בדף תודה כאשר האתר כבר נוצר.
- תיקון: אפשרות רישום של WP core מסונכרנת בהפעלת ה-plugin ובשמירת ההגדרות.
- תיקון: הוספת מגן תפוגה (Null expiration guard) ב-`calculate_expiration` לתאימות עם PHP 8.4.
- תיקון: רישומים כפולים נחסמים כאשר ללקוח כבר יש מנוי פעיל.
- תיקון: הוספת בדיקת null עבור `date_expiration` בתשלום.
- תיקון: תהליך הקמת האתר (Site provisioning) מחוזק — מגבלות, הסקת מנוי, קידום דומיין.
- תיקון: תווית סטטוס של בדיקת התקנה מוקדמת מתוקנת ל-NOT Activated כאשר הבדיקה נכשלת.
- תיקון: דומיין התשלום משמש עבור כתובות ה-URL של אימות המייל.
- תיקון: התחברות אוטומטית לאחר תשלום כאשר אין שדה סיסמה.
- תיקון: מנויים חינם אינם פוקעים יותר — מטופלים כחיים.
- תיקון: שער אימות המייל שומר את פרסום האתר עד שהלקוח מאמת את המייל.
- תיקון: נתיב בסיס ודרך הזהות של נקודת קצה SES v2 API תוקנו.
- תיקון: ה-hook `wu_inline_login_error` מופק בבלוק ה-catch של ה-pre-submit.
