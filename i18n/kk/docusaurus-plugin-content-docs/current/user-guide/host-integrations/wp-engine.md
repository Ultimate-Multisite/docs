---
title: WP Engine Интеграциясы
sidebar_position: 2
_i18n_hash: ceeee20432439d8afb3002dd0dd1ff12
---
# WP Engine Интеграция

## Обзор {#overview}
WP Engine — это премиум-хостинг платформа для WordPress, которая обеспечивает оптимизированную производительность, безопасность и масштабируемость сайтов на WordPress. Эта интеграция позволяет автоматически синхронизировать домены между Ultimate Multisite и WP Engine.

## Возможности {#features}
- Автоматическая синхронизация доменов
- Поддержка поддоменов для мультисайтовых установок
- Бесшовная интеграция с существующими системами WP Engine

## Требования {#requirements}
Интеграция автоматически определяет, размещаете ли вы сайт на WP Engine, и использует встроенный API WP Engine. Дополнительная настройка не требуется, если плагин WP Engine активен и правильно настроен.

Однако, если вам нужно вручную настроить интеграцию, вы можете определить одну из этих констант в вашем файле `wp-config.php`:

```php
define('WPE_APIKEY', 'your_api_key'); // Предпочтительный метод
// ИЛИ
define('WPE_API', 'your_api_key'); // Альтернативный метод
```

## Инструкции по настройке {#setup-instructions}

### 1. Проверьте плагин WP Engine {#1-verify-wp-engine-plugin}

Если вы размещаете сайт на WP Engine, плагин WP Engine должен быть уже установлен и активирован. Убедитесь, что:

1. Плагин WP Engine активен
2. Существует файл `wp-content/mu-plugins/wpengine-common/class-wpeapi.php`

### 2. Включите интеграцию {#2-enable-the-integration}

1. В админке WordPress перейдите в Ultimate Multisite > Settings (Настройки)
2. Перейдите на вкладку "Domain Mapping" (Сопоставление доменов)
3. Прокрутите вниз до раздела "Host Integrations" (Интеграции хостинга)
4. Включите интеграцию WP Engine
5. Нажмите "Save Changes" (Сохранить изменения)

## Как это работает {#how-it-works}

### Синхронизация доменов {#domain-syncing}

Когда домен сопоставляется в Ultimate Multisite:

1. Интеграция использует API WP Engine для добавления домена в вашу установку WP Engine
2. WP Engine обрабатывает конфигурацию домена и выдачу SSL-сертификата
3. Когда сопоставление домена удаляется, интеграция удалит этот домен из WP Engine

### Поддержка поддоменов {#subdomain-support}

Субдомен мультисайт орнату үшін:

1. Интеграция жаңа сайт жасалған кезде әр субдоменді WP Engine-ге қосады.
2. WP Engine субдоменді конфигурациялауды басқарады.
3. Сайт өшірілгенде, интеграция субдоменді WP Engine-ден алып тастайды.

## Маңызды ескертулер {#important-notes}

### Жұлдызша (Wildcard) домендер {#wildcard-domains}

Субдомен мультисайт орнату үшін жұлдызша домен конфигурациясын сұрау үшін WP Engine қолдау қызметіне хабарласу ұсынылады. Бұл барлық субдомендерді жеке қосу қажетсіз, автоматты түрде жұмыс істеуге мүмкіндік береді.

### SSL сертификаттары {#ssl-certificates}

Бұл интеграция арқылы қосылған барлық домендер үшін WP Engine SSL сертификатын беру мен жаңартуды автоматты түрде басқарады. Қошимді конфигурация қажет емес.

## Мәселелерді шешу (Troubleshooting) {#troubleshooting}

### API қосылу мәселелері {#api-connection-issues}
- WP Engine плагині белсенді және дұрыс конфигурацияланғанын тексеріңіз.
- Егер сіз API кілтін қолмен анықтасаңыз, оның дұрыс екенін тексеріңіз.
- API-де қиындық туындаса, WP Engine қолдау қызметіне хабарласыңыз.

### Домен қосылмаған {#domain-not-added}
- Қате хабарламалар үшін Ultimate Multisite логин тексеріңіз.
- Домендің WP Engine-ге әлі қосылмағанын растаңыз.
- Сіз қосатын домен санын WP Engine жоспарыңыз қолдайтынын қамтамасыз етіңіз.

### Субдомен мәселелері {#subdomain-issues}
- Егер субдомендер жұмыс істемесе, жұлдызша домен конфигурациясын сұрау үшін WP Engine қолдау қызметіне хабарласыңыз.
- Негізгі домен және субдомендер үшін DNS параметрлеріңіз дұрыс бапталып тұрғанын тексеріңіз.
